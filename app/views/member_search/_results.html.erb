<%- if @results == nil -%>
	<h1><%= _('Member Search') %></h1>
	<div class="activity_box">
		<p></p>
       	<p><strong>The member search is an easy way to find people who have similar interests, professions, and/or locations to you. </strong></p>
		<p>Just fill in or select any of the fields to the right then click submit to explore the amazing people who are Teachers Without Border's members and start connecting with new people around the world.</p>
	</div>
<% elsif @results.empty? %>
		<!-- don't do anything -->
<% else %>
	<h1><%= _("Member Search Results") %></h1>
	<ul class='search_results'>
    	<% @results.each do |result| %>
		    <li><%= link_to result.full_name, profile_path(result) %><%= _('(user)') %> <%= h result.why_joined %></li>
     	<% end %>
	</ul>
	<%= will_paginate @results, :previous_label => _('&laquo; Previous'), :next_label => _('Next &raquo;') %>
<% end %>
